projection;
strict ( 2 );
use side effects;
use draft;
define behavior for ZC_ROOT_ADMIN //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Approve;
  use action Reject;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

}

